for (i = 0; i < 10; i++) {
   let a, b;
   a = square = (i*i);
   b = cube = (i*i*i);

   console.log(square, cube);

   average = ((a + b)/2);
   console.log(average);

   let calculate = [average];
   console.log(calculate);
};
